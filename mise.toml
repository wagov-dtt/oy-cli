[tools]
python = "3.14"
uv = "0.10.0"

[tasks]
sync = "uv sync"
fmt = "uv run ruff format ."
lint = "uv run ruff check ."
build = "uv build"
check = [
  "uv run ruff check .",
  "uv run python -m py_compile src/oy_cli/__init__.py",
  "uv run oy --help",
]
